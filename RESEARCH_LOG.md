# 研究ログ（追記式・失敗案を保持）

## 2026-09-08 — 現行実装監査とオフライン比較基盤

- 仮説: 見逃しはEMA追従だけでなく、gate/閾値下限/特徴無変動/窓幅/scoreの組合せに起因し得る。周期性不足は未確定。
- 変更: 4特徴ログ拡張、freeze、同一train状態からの比較、>=2/1、正確なラベル境界と窓終了latency、Jain/CV/ACF/FFT、条件config・人工fixture・PCAP再解析・図・provenance・テスト。
- 理由: 既存実装は特徴の定義/窓幅/入力scopeが原稿・背景説明と一致しない箇所があり、原因を判断する前に比較単位を固定する必要がある。
- 必要実験: EXPERIMENT_PLAN.mdの既存capture再解析。元81.8%集計とrunの対応回収。独立normal random/normal periodic、25 ms/4 s、dynamic/freeze。
- 結果: 実ネットワーク本実験/長時間性能評価は未実施。短い単体・回帰・人工CSV統合テストのみ。検知率改善、TCP改善、XDP性能の主張は未検証。
- 解釈: 現行既定の分類出力を変更前commitと比較する回帰テストで互換性を確認する。人工fixtureで出た率は動作確認値であり研究成果の実測値ではない。
- 次の仮説: gate抑制と下限0の寄与を特徴別ログで分離すれば、>=1変更で十分か判断できる可能性。
- 新規性への示唆: 追加特徴の最小性、正常周期の除外、jitter/costの比較が候補。単にJainを使うことや周期性を検知することは新規性として主張しない。

### 保留・採用しない案

- 「最初から1 Hzを見れば解決」: 特定周期への過適合と既存random生成の1 Hz抑制選択が交絡するため採用しない。
- 「JainとCVを独立の追加情報として併用」: 同じ間隔列でJ=1/(1+CV²)なので根拠にならない。式の比較候補として残す。
- 「UDPしか入力しないBaselineでTCP FPRを保証」: 入力対象が異なるため不可。空窓の結果と全TCP評価を分離する。
- 「最初のUDPを実験開始とみなす」: 無通信開始区間と攻撃時刻をずらす可能性。明示epochアダプタへ変更。
- PCAP読み出しエラー(return code 1)を空正常入力とみなす旧挙動: エラーを伝播する修正を加えた。
- 原稿と再現器のsource-port/flow_id差は未統合。仕様を黙って変えず別profileの検討事項とする。

## 2026-09-09 — 検証と引渡し

- 仮説: 元判定と異なる結果が出た場合、研究仮説の前に実装差・時刻/ラベル誤りを除外する。
- 変更/理由: 変更前commitの全既存出力列を比較する回帰テスト、freeze不変、score==2、下限0、packet gate、境界窓、複数イベント/未検知、Jain/CV同値、未来非参照、破損PCAP拒否、校正参照必須、同一seed再現、25 ms/4 s統合テストを追加。fixtureの条件記録を各生成法に合わせた。
- 必要実験: 保存PCAPの実時刻原点を回収して再解析。source-port版と現行flow_id版の相違は本研究の未確定前提として残す。
- 結果: 14件の短いテストが成功。人工4条件のCSV解析と診断図生成を確認。実ネットワーク本実験・長時間性能評価は行っていない。
- 解釈: 分析コードの動作・互換性を確認した段階。元81.8%の再現・提案手法の実データ精度・処理負荷は未検証。
- 次の仮説: 同一capture上のdynamic/freezeと>=1/2の差を見れば、時間特徴を追加する前に改善余地と誤検知の増分を定量化できる。
- 新規性への示唆: 依然として実験待ち。正常periodicとLDoSを同じ時間配置にした人工例ではJain単独では識別できないので、複合特徴を検討する分岐を保持。


## 2026-09-09 — ユーザー提示の元論文と公開C実装を照合

- 仮説: 現行Pythonと原手法の差が、見逃しの原因解釈に影響している可能性。
- 変更/理由: PDF p.24の公開先とGitHub HEAD 6f118ae22de22df6274a5a06ea25d99ec1d69989を確認し、docs/ORIGINAL_PAPER_AUDIT.mdへ本文・C・Pythonの差を記録。検知コードは変更せず。
- 必要実験/確認: 論文実験時commit、map初期化、IAT尺度、payload/flow定義の回収。実験PC不在のため本実験は保留。
- 結果: 正の閾値floor、source-port、100 packet/CPU上限、IAT式、総scoreと更新順序などの差を静的に確認。等間隔10 packetの公開IAT整数式はu64 wrapを生じる小例を確認（実機測定ではない）。
- 解釈: 「下限0で見逃す」は現行Pythonについての仮説であり、元検知器の性質とは断定できない。論文Algorithm2と公開コードの更新規則も同一ではない。
- 次の仮説: P0現行Python・P1論文準拠・P2公開コードを区別することで、実装差と特徴集合の限界を分離できる。
- 新規性への示唆: 元手法のblind spotの主張前に対象版の同定が必要。比較の再現性問題を検知改善の新規性と混同しない。


## ユーザー補足 — 原稿の実行窓は25 ms

- 確認事項: ユーザーが「実行時には25msでやってた」と明示した。
- 変更/理由: 原稿実験の25 msと、現行コードの既定4 sを区別する補足を監査記録へ追記。検知コードの変更なし。
- 結果/解釈: 25 ms実行はユーザー確認済みとして扱う。既定値4 sは原稿の窓幅不一致の根拠にはならない。実行設定ファイルとの対応は再現性の記録として別途整える。
- 次の確認: 閾値下限・特徴量スケール・更新規則等が当時の評価でどう設定されていたかを照合する。元手法との全差分が解消したとはまだ判断しない。
- 新規性への示唆: 原稿への批判をデフォルト窓幅に基づけず、実際の判定に影響する差に絞る。
