# Research Implementation Principles

正確に、わかりやすく記述する。このファイルは研究全体で長期的に維持する行動原則であり、具体的な特徴量・実験条件・現在のPhaseは下記の文書で管理する。

## Research execution principles

- 長時間の推測や文献調査を続けるより、十分に妥当で反証可能な仮説ができたら最小PoCによる早期検証を優先する。調査は次の判断に必要な範囲から行う。
- 仮説を結論として扱わない。記述では、(1) 既存研究・既知の事実、(2) 仮説、(3) 実験による観測結果、(4) 観測から支持された結論を明確に区別する。出典と成立条件を付ける。
- 新しい検知アイデアは、まず最小限の実装で価値を確認する。単純な閾値・統計量で十分なら、不要なMLや複雑なモデルを導入しない。
- 推定処理が必要な場合、可能なら最初にground truthを使い、その情報自体に検知価値があるか確認する。価値を確認した後にpassive inferenceや複雑な推定へ進む。ground truthを取得できない場合は代替観測と限界を明記する。
- 攻撃条件だけでなく、厳しい正常条件と必ず比較する。単にTCP性能低下を検知しているのか、LDoSに特徴的な現象を検知しているのかを区別する。周期的であることや性能低下だけで攻撃ラベルを定義しない。
- 結果が仮説を支持しない場合、予定した研究ストーリーに合わせない。negative resultも保存し、必要なら仮説・方法・研究方向を変更または棄却する。
- competing hypothesesがある場合、最短でそれらを区別できる実験を優先する。実装差、入力差、ラベル誤り、校正条件を、新しい検知原理の効果と混同しない。
- 新規性は実験結果と先行研究調査の両方を確認してから主張する。「世界初」「未研究」などを根拠なく断定しない。未照合の文献メモを検証済みの出典として扱わない。
- 実装・評価を再現可能にする。実験ID、条件、seed、実行コマンド、コード版（未コミット差分を含む）、環境、ログ形式、入力データ、解析方法・解析版を可能な限り記録する。不明な情報は推測で補完しない。
- 校正・閾値選択用データと最終評価用データを分離する。同一runの重複窓を独立試行とみなさない。未検知・欠測・失敗を除外して見かけの性能を上げない。
- 人工fixtureの動作確認、オフライン分類、本物のネットワーク実験、実機の処理性能を区別する。実装済み・テスト成功は研究仮説の成立を意味しない。

## Machine constraint

Macは研究実装用PCとして扱う。実験用PCでは、ユーザーが配置したGit管理外の `machine_role.local.json` に `"role": "experiment"` があり、実際のホストがLinuxの場合に実験PCの役割を適用する。未指定・不正・Mac上では実装PCとして扱う。設定値だけから実験環境の準備完了とは判断しない。

- 実装PCの主な作業はコード理解、実装、オフライン解析、ログ解析、可視化、実験スクリプト作成、実験条件作成、実験PCで実行するコマンド準備とする。
- 短時間・小規模の単体テストや人工データによる動作確認は行ってよい。大きな解析は対象量と資源使用を見積もり、必要なら実験PC向けに準備する。
- 実装PCでは長時間の性能評価、大規模パラメータ探索、本格的なMininet実験は原則として実験用PC向けに準備するだけにする。実験用PCへの持ち込みで再現できる条件・スクリプト・出力仕様を残す。

- 実験PCでは現在のGate内の実装・解析と、READYになった実験計画に従うネットワーク実験・性能測定を行える。ベースラインは固定し、条件やパラメータの変更理由、seed、複数試行、raw log / PCAP、失敗結果を保存する。結果によってGateを判断し、Phase 2へ自動的に進まない。

## Reading order and source of truth

作業開始時は次の順に読み、対象ディレクトリに別の `AGENTS.md` があれば併せて確認する。

1. この `AGENTS.md`: 長期的な行動原則。
2. [research_direction.md](docs/research_direction.md): 現在の研究方向とPhase間の分岐。
3. [current_phase.md](docs/current_phase.md): 現在の最優先作業、許可された実装範囲、保留事項、次のDecision Gate。
4. [research_log.md](docs/research_log.md): 最新の判断と、その根拠となるentry・参照資料。関連する過去entryも読む。
5. [experiment_plan.md](docs/experiment_plan.md): 対象実験の準備状態、条件、コマンド、成果物。
6. 各文書から参照する既存の詳細資料と対象コード。配置・同期方法は [README.md](README.md) と [引渡し手順](docs/HANDOFF.md) を確認する。

現在の方針は上記 `docs/` を正本とし、既存メモの予定日やTODOだけを理由に次の実装へ進まない。事実について資料が矛盾する場合は一方を黙って採用せず、根拠と未解決点をログに残し、その事実に依存しない最小作業を進める。ユーザーの明示指示を優先する。

## Decision Gates and updates

- `Hypothesis → Minimal PoC → Result → GO / REVISE / NO-GO` で進める。GOは次の限定した検証を許可する判断であり、Phase全体の成功や完成版への包括的な許可ではない。
- 証拠不足は未判定とする。日付到来、実装完了、予定消化だけでGateを通過しない。
- 現在の `Do Not Implement Yet` を越えて先行実装しない。Gate判断をログに残してから `current_phase.md` の範囲を更新する。
- このファイルは数ヶ月単位でほぼ固定。研究方向が変われば `research_direction.md`、Phase/Gateが変われば `current_phase.md`、重要な結果が出れば `research_log.md` へ追記、実験内容が決まれば `experiment_plan.md` を更新する。
- 作業終了時に、実施内容・観測・未確定事項・次の最小作業を対応文書へ反映する。過去ログの結論訂正は、元entryへの参照付きで追記する。

## Repository preservation

既存コード、原稿、設定、生データ、未コミット変更を保持する。実装の通常編集先はこのGitリポジトリとし、アーカイブや原稿付属コピーを現行実装と混同しない。重複実装・重複ログの正本を増やさず、詳細記録を参照する。ユーザーの依頼範囲外の大規模整理、結果の上書き、実験PCや外部サービスへの変更を自動的に行わない。

研究ハーネス5文書の正本は、このGitリポジトリの `AGENTS.md` と `docs/` にある。親ワークスペース側は案内のみ。Gitで同期するときは5文書と必要なコードを明示してcommit対象に含める。未コミット状態の引渡しは [HANDOFF.md](docs/HANDOFF.md) のbundle方式を使う。
