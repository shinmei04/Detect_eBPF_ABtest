# 新規性候補と弱点

更新: 2026-09-08。主張は未検証。実験結果を受けて追記し、不成立案も削除しない。

| 観点 | 記録 |
|---|---|
| 既存検知器との差分 | 4特徴の窓内統計へburst開始間隔の時間構造を補完する設計候補。まずgate・閾値・窓幅で説明できる部分を除く。 |
| 単純改善との差分 | >=1に対するFPR/TPR/latencyの追加改善が必要。>=1で十分なら時間構造追加の主張を撤回・再検討。 |
| 既存研究との差分 | 周期性検知自体の新規性は主張しない。ACF/FFT/既存軽量手法との等条件比較・正常周期制御・計算状態上限の差が未検証。 |
| 新しい観察 | 現行コードは複数の4特徴定義を持つ。25 ms bucketと判定窓4 s、source-portとIP/port ID、gateとraw flagを区別しないと原因を誤帰属する。これは監査結果で、攻撃検知の新発見とは別。 |
| 設計上の工夫 | 同じ正常train状態を複製しdynamic/freezeを比較。判定前閾値と更新理由、明示原点、transition分離、未検知null、因果的時間特徴を統一ログへ保存。 |
| 新規性として弱い点 | JainとCVは単調変換。Jain単独は正常周期も高くなる。Python referenceの軽量性はXDP実現性の証明ではない。手法別入力（間隔列とcount列）の差も交絡。 |
| 主張候補 | 「特定の短時間統計型検知器の、単純閾値緩和では解消しないblind spotを、正常周期・jitterを考慮した少量状態の時間特徴で補完」— 全て比較実験待ち。 |

## 既存研究の確認状況

LDoSの先行研究としてKuzmanovic/​KnightlyのSIGCOMM 2003論文を確認した：[ACM原著ページ](https://doi.org/10.1145/863955.863966)。これだけで防御の新規性は評価できない。

対象実装の比較入口：[著者公開XDPコード](https://raw.githubusercontent.com/mahmoudelzoghbi92/LDoS-Detection-eBPF-XDP/main/xdp_prog.c)。現行ローカル再現器を原著完全再現と呼ばない。今回のコード実装位置・式の記述はローカルソースに基づく。

未完了: 周期/スペクトル/自己相関によるLDoS検知の原著本文比較、Jain利用の先行例、データセット/誤検知定義/計算量/実装環境を列にした比較表。関連研究の網羅調査を済ませたとは主張しない。

## 軽量化の候補（未実装・未測定）

間隔sum/sum_sqと件数によるJain更新は、期限切れ間隔を除くring bufferと組み合わせられる。平方根を避けた整数比較の候補になるが、sum²のoverflow、窓境界、timestamp精度、map上限、flow churn、per-CPU統合を検証する必要がある。

現行referenceは窓ごとの再集計。Jain/CVの間隔集計はmに比例、ACFはサンプル数×lag数、FFTは通常n log nの演算規模が目安だが、CPU/メモリ実測とXDP verifier通過は別の証拠。Python実装の速度だけで採否を決めない。


## 2026-09-09 元論文照合による留保

PDFに記された公開リポジトリを確認。詳細はdocs/ORIGINAL_PAPER_AUDIT.md。閾値下限・IAT整数式・packet上限・更新条件・flow識別に本文/C/Pythonの差がある。現行Pythonの見逃しから直接「元論文検知器の限界」と主張しない。Jain追加を議論する前に、再現プロファイルと入力/数値尺度を固定する。
